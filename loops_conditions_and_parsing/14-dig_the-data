#!/usr/bin/env bash
#write a Bash script that groups visitors by IP and HTTP status code
LOG_FILE="apache-access.log"
awk '{count[$1" "$9]++} END {for (i in count) print count[i], i}' "$LOG_FILE" | sort -rn
